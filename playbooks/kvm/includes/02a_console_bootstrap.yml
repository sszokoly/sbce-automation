- name: "Enter your choice [1 - 4] : 1"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "1"
    no_enter: true
  tags:
    - step02
    - step02a
    - step02a1

- name: Send ENTER and wait 10 seconds
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: 10
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a2

- name: "IP Mode: {{ args.ipmode }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ipmode }}"
    no_enter: true
  when:
    - args.ipmode is defined
    - args.ipmode is not none
    - args.ipmode | string | lower != "null"
    - args.ipmode | string | length > 0
  tags:
    - step02
    - step02a
    - step02a3

- name: Send ipmode ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a4

- name: "Appliance Type: {{ args.apptype }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.apptype }}"
    no_enter: true
  when: args.apptype is defined and (args.apptype | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a5

- name: Send apptype ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a6

- name: "Network Passphrase: {{ args.nwpass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.nwpass }}"
    no_enter: true
  when:
    - args.apptype in ['EMS', 'SBCE']
    - args.nwpass
  tags:
    - step02
    - step02a
    - step02a7

- name: Send nwpass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a8

- name: "Appliance Name: {{ args.appname }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.appname }}"
    no_enter: true
  when: args.appname is defined and (args.appname | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a9

- name: Send appname ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a10

- name: "Installation Type: {{ args.ems_inst_type }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ems_inst_type }}"
    no_enter: true
  when:
    - args.apptype == 'EMS'
    - args.ems_inst_type
  tags:
    - step02
    - step02a
    - step02a11

- name: Send ems_inst_type ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  when:
    - args.apptype == 'EMS'
    - args.ems_inst_type
  tags:
    - step02
    - step02a
    - step02a12

- name: "Management IP address: {{ args.ip0 }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ip0 }}"
    no_enter: true
  when: args.ip0 is defined and (args.ip0 | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a13

- name: Send ip0 ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a14

- name: "Management subnet mask: {{ args.netmask0 }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.netmask0 }}"
    no_enter: true
  when: args.netmask0 is defined and (args.netmask0 | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a15

- name: Send netmask0 ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a16

- name: "Management Gateway IP address (ipv4): {{ args.gateway }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.gateway }}"
    no_enter: true
  when: args.gateway is defined and (args.gateway | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a17

- name: Send gateway ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a18

- name: "Management IP address (ipv6): {{ args.ipv6address0 }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ipv6address0 }}"
    no_enter: true
  when: args.ipv6address0 is defined and (args.ipv6address0 | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a19

- name: Send ipv6address0 ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a20

- name: "Management subnet network prefix length: {{ args.ipv6prefix0 }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ipv6prefix0 }}"
    no_enter: true
  when: args.ipv6prefix0 is defined and (args.ipv6prefix0 | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a21

- name: Send ipv6prefix0 ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a22

- name: "Management Gateway IP Address (ipv6): {{ args.ipv6gateway }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ipv6gateway }}"
    no_enter: true
  when: args.ipv6gateway is defined and (args.ipv6gateway | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a23

- name: Send ipv6gateway ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a24

- name: "EMS IP address (ipv4): {{ args.emsip }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.emsip }}"
    no_enter: true
  when:
    - args.apptype == 'SBCE' or (args.apptype == 'EMS' and args.ems_inst_type == 'secondary')
    - args.emsip is defined and (args.emsip | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a25

- name: Send args.emsip ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  when:
    - args.apptype == 'SBCE' or (args.apptype == 'EMS' and args.ems_inst_type == 'secondary')
    - args.emsip is defined and (args.emsip | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a26

- name: "EMS IP address (ipv6): {{ args.emsip_v6 }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.emsip_v6 }}"
    no_enter: true
  when:
    - args.apptype == 'SBCE' or (args.apptype == 'EMS' and args.ems_inst_type == 'secondary')
    - args.emsip_v6 is defined and (args.emsip_v6 | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a27

- name: Send args.emsip_v6 ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  when:
    - args.apptype == 'SBCE' or (args.apptype == 'EMS' and args.ems_inst_type == 'secondary')
    - args.emsip_v6 is defined and (args.emsip_v6 | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a28

- name: "NTP Server IP Address (ipv4): 127.127.1.0"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
  tags:
    - step02
    - step02a
    - step02a29

- name: "NTP Server IP Address (ipv6):"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
  tags:
    - step02
    - step02a
    - step02a30

- name: "List of DNS Servers: {{ args.dns }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.dns }}"
    no_enter: true
  when: args.dns is defined and (args.dns | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a31

- name: Send dns ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a32

- name: "Domain Suffix: {{ args.domain_suffix }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.domain_suffix }}"
    no_enter: true
  when: args.domain_suffix is defined and (args.domain_suffix | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a33

- name: Send domain_suffix ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a34

- name: "Enter 'Y' if the above infirmation is correct: Y"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "Y"
    no_enter: true
    sleep_time: "{{ sleep_time }}"
  tags:
    - step02
    - step02a
    - step02a35

- name: Send confirmation ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a36

- name: "First and Last Name: {{ args.first_last_name }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.first_last_name }}"
    no_enter: true
  when: args.first_last_name is defined and (args.first_last_name | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a37

- name: Send first_last_name ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a38

- name: "Organizational Unit: {{ args.organizational_unit }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.organizational_unit }}"
    no_enter: true
  when: args.organizational_unit is defined and (args.organizational_unit | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a39

- name: Send organizational_unit ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a40

- name: "Organization: {{ args.organization }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.organization }}"
    no_enter: true
  when: args.organization is defined and (args.organization | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a41

- name: Send organization ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a42

- name: "City or Locality: {{ args.locality }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.locality }}"
    no_enter: true
  when: args.locality is defined and (args.locality | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a43

- name: Send locality ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a44

- name: "State or Province: {{ args.state_province }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.state_province }}"
    no_enter: true
  when: args.state_province is defined and (args.state_province | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a45

- name: Send state_province ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a46

- name: "Country Code ( 2 letter code ): {{ args.country }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.country }}"
    no_enter: true
  when: args.country is defined and (args.country | string | length) > 0
  tags:
    - step02
    - step02a
    - step02a47

- name: Send country ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a48

- name: "Enter 'Y' if the above infirmation is correct: Y"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "Y"
    no_enter: true
    sleep_time: "{{ sleep_time }}"
  tags:
    - step02
    - step02a
    - step02a49

- name: Send confirmation ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a50

- name: "Please select a continent or ocean (11 = none): 11"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "11"
    no_enter: true
  tags:
    - step02
    - step02a
    - step02a51

- name: Confirm continent (default Yes)
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a52

- name: "Please enter the desired value of the TZ environment variable: GMT-4"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "GMT-4"
    no_enter: true
  tags:
    - step02
    - step02a
    - step02a53

- name: Confirm timezone (default Yes)
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a54

- name: "Is the above information OK? (1 Yes, 2 No): 1"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "1"
    no_enter: true
  tags:
    - step02
    - step02a
    - step02a55

- name: Send ENTER and wait 90 seconds
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: 90
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a56

- name: "New Password for 'root' user: {{ args.rootpass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.rootpass }}"
    no_enter: true
  when: args.rootpass is defined and (args.rootpass | string | length) > 0
  no_log: true
  tags:
    - step02
    - step02a
    - step02a57

- name: Send rootpass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a58

- name: "Retype new password for 'root' user: {{ args.rootpass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.rootpass }}"
    no_enter: true
  when: args.rootpass is defined and (args.rootpass | string | length) > 0
  no_log: true
  tags:
    - step02
    - step02a
    - step02a59

- name: Send rootpass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a60

- name: "New Password for 'ipcs' user: {{ args.ipcspass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ipcspass }}"
    no_enter: true
  when: args.ipcspass is defined and (args.ipcspass | string | length) > 0
  no_log: true
  tags:
    - step02
    - step02a
    - step02a61

- name: Send ipcspass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a62

- name: "Retype new password for 'ipcs' user: {{ args.ipcspass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.ipcspass }}"
    no_enter: true
  when: args.ipcspass is defined and (args.ipcspass | string | length) > 0
  no_log: true
  tags:
    - step02
    - step02a
    - step02a63

- name: Send ipcspass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a64

- name: "New Password for 'grub' user: {{ args.grubpass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.grubpass }}"
    no_enter: true
  when: args.grubpass is defined and (args.grubpass | string | length) > 0
  no_log: true
  tags:
    - step02
    - step02a
    - step02a65

- name: Send grubpass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a66

- name: "Retype new password for 'grub' user: {{ args.grubpass }}"
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    string_send: "{{ args.grubpass }}"
    no_enter: true
  when: args.grubpass is defined and (args.grubpass | string | length) > 0
  no_log: true
  tags:
    - step02
    - step02a
    - step02a67

- name: Send grubpass ENTER
  kvm_guest_sendkey:
    name: "{{ args['vmname'] }}"
    keys_send:
      - ENTER
    sleep_time: "{{ sleep_time }}"
  no_log: true
  changed_when: false
  tags:
    - step02
    - step02a
    - step02a68